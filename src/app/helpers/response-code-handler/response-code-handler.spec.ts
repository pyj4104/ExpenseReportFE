import { ResponseCodeHandler } from './response-code-handler';

describe('ResponseCodeHandler', () => {
	it('should create an instance', () => {
		expect(new ResponseCodeHandler()).toBeTruthy();
	});
});
